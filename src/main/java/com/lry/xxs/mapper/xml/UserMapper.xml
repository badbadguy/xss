<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lry.xxs.mapper.UserMapper">

    <!--表名 -->
    <sql id="tableName">
        USER
    </sql>

    <!-- 字段 -->
    <sql id="Field">
        USER_NAME,
        USER_NICKNAME,
        USER_IMAGE,
        USER_PASSWORD,
        USER_TYPE,
        CREATTIME,
        UPDATETIME,
        USER_ID
    </sql>

    <!-- 字段值 -->
    <sql id="FieldValue">
        #{USER_NAME},
        #{USER_NICKNAME},
        #{USER_IMAGE},
        #{USER_PASSWORD},
        #{USER_TYPE},
        #{CREATTIME},
        #{UPDATETIME},
        #{USER_ID}
    </sql>

    <!-- 新增-->
    <insert id="save" parameterType="pd">
        insert into
        <include refid="tableName"></include>
        (
        <include refid="Field"></include>
        ) values (
        <include refid="FieldValue"></include>
        )
    </insert>

    <!-- 删除-->
    <delete id="delete" parameterType="pd">
        delete from
        <include refid="tableName"></include>
        where
        USER_ID = #{USER_ID}
    </delete>

    <!-- 修改 -->
    <update id="edit" parameterType="pd">
        update
        <include refid="tableName"></include>
        set
        <if test="USER_NAME!= null and USER_NAME != ''">
            USER_NAME = #{USER_NAME},
        </if>
        <if test="USER_NICKNAME!= null and USER_NICKNAME != ''">
            USER_NICKNAME = #{USER_NICKNAME},
        </if>
        <if test="USER_IMAGE!= null and USER_IMAGE != ''">
            USER_IMAGE = #{USER_IMAGE},
        </if>
        <if test="USER_PASSWORD!= null and USER_PASSWORD != ''">
            USER_PASSWORD = #{USER_PASSWORD},
        </if>
        <if test="USER_TYPE!= null and USER_TYPE != ''">
            USER_TYPE = #{USER_TYPE},
        </if>
        <if test="CREATTIME!= null and CREATTIME != ''">
            CREATTIME = #{CREATTIME},
        </if>
        <if test="UPDATETIME!= null and UPDATETIME != ''">
            UPDATETIME = #{UPDATETIME},
        </if>
        USER_ID = USER_ID
        where
        USER_ID = #{USER_ID}
    </update>

    <!-- 通过ID获取数据 -->
    <select id="findById" parameterType="pd" resultType="pd">
        select
        <include refid="Field"></include>
        from
        <include refid="tableName"></include>
        where
        USER_ID = #{USER_ID}
    </select>

    <!-- 列表 -->
    <select id="datalistPage" parameterType="page" resultType="pd">
        select
        <include refid="Field"></include>
        from
        <include refid="tableName"></include>
        where 1=1
        <if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
            and
            (
            <!--	根据需求自己加检索条件
                字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
                 or
                字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
            -->
            )
        </if>
        <if test="pd.USER_NAME!= null and pd.USER_NAME != ''">
            and USER_NAME = #{pd.USER_NAME}
        </if>
        <if test="pd.USER_NICKNAME!= null and pd.USER_NICKNAME != ''">
            and USER_NICKNAME = #{pd.USER_NICKNAME}
        </if>
        <if test="pd.USER_IMAGE!= null and pd.USER_IMAGE != ''">
            and USER_IMAGE = #{pd.USER_IMAGE}
        </if>
        <if test="pd.USER_PASSWORD!= null and pd.USER_PASSWORD != ''">
            and USER_PASSWORD = #{pd.USER_PASSWORD}
        </if>
        <if test="pd.USER_TYPE!= null and pd.USER_TYPE != ''">
            and USER_TYPE = #{pd.USER_TYPE}
        </if>
        <if test="pd.CREATTIME!= null and pd.CREATTIME != ''">
            and CREATTIME = #{pd.CREATTIME}
        </if>
        <if test="pd.UPDATETIME!= null and pd.UPDATETIME != ''">
            and UPDATETIME = #{pd.UPDATETIME}
        </if>
    </select>

    <!-- 列表(全部) -->
    <select id="listAll" parameterType="pd" resultType="pd">
        select
        <include refid="Field"></include>
        from
        <include refid="tableName"></include>
    </select>

    <!-- 批量删除 -->
    <delete id="deleteAll" parameterType="String">
        delete from
        <include refid="tableName"></include>
        where
        USER_ID in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- fh313596790qq(青苔) -->
</mapper>